#include "main.h"
#include <string.h>
/**
 * _strcat - function strcat
 * @dest: first number
 * @src: second number
 * Return: a string
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
