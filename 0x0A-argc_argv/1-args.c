#include <stdio.h>

/**
 * main - pritnts its name
 * @argc: argc parameter
 * @argv: an array of a command listed
 * Return: returns 0
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argc - 1);
return (0);
}
